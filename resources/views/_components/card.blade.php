<div class="card h-100">
    <img src="{{ $imagen }}" class="card-img-top" alt="{{ $titulo }}">
    <div class="card-body">
        <h5 class="card-title">{{ $titulo }}</h5>
        <p class="card-text">{{ $contenido }}</p>
        <a href="{{ $botonLink }}" class="btn btn-primary">{{ $botonTexto }}</a>
    </div>
</div>
